#!/bin/bash

# クイズアプリケーション再起動スクリプト

echo "=========================================="
echo "  クイズアプリケーション再起動"
echo "=========================================="
echo ""

# docker-compose.ymlの存在確認
if [ ! -f "docker-compose.yml" ]; then
    echo "❌ エラー: docker-compose.ymlが見つかりません"
    echo "   プロジェクトのルートディレクトリで実行してください"
    exit 1
fi

# コンテナを再起動
echo "🔄 Dockerコンテナを再起動しています..."
docker-compose restart

if [ $? -eq 0 ]; then
    echo ""
    echo "✅ アプリケーションを再起動しました！"
    echo ""
    echo "🌐 アクセス: http://localhost:5000"
    echo ""
else
    echo ""
    echo "❌ エラー: アプリケーションの再起動に失敗しました"
    exit 1
fi

